'''
✅Функции для работы с БД
Файл содержит CRUD-операции (Create, Read, Update, Delete) для работы с пользователями

🟢HTTPException - теперь API будет возвращать HTTP-ошибки вместо JSON-ответа
🟢AsyncSession - асинхронная сессия для работы с БД
🟢select - SQL-запрос для выборки данных
🟢IntegrityError - ошибка целостности БД (например, если пытаемся создать дубликат email)
🟢User - модель пользователя из models.py
🟢UserCreate - схема с валидацией данных из schemas.py

❓Почему для получения одного пользователя мы используем execute, а для удаления get?
Разница между execute(select()) и get():

📌session.execute(select(User).where(User.id == user_id))

Использует SQL-запрос (SELECT * FROM users WHERE id = user_id).
Требует дополнительного извлечения данных (scalar_one_or_none()).
Гибкий метод, позволяет строить сложные запросы (например, фильтрация, сортировка).

📌session.get(User, user_id)

Использует метод SQLAlchemy, который сразу ищет по первичному ключу (id).
Оптимизирован для быстрого поиска по id.
Не требует select() - сразу возвращает объект.

❓Можно ли использовать обычный SQL-запрос?
Да, можно! SQLAlchemy позволяет выполнять чистый SQL с помощью session.execute("SQL запрос")
❌Так делать нельзя!
SQL-инъекции - если user_id передадут в виде "; DROP TABLE users; --", то БД может удалить всю таблицу
Нет поддержки ORM - данные возвращаются как кортежи, а не как объект User
Проблемы с переносимостью - ORM автоматически адаптируется под разные базы данных, а сырой SQL - нет
'''